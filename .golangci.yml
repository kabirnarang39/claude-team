version: "2"

run:
  timeout: 5m

linters:
  disable:
    - errcheck
  enable:
    - govet
    - ineffassign
    - staticcheck
    - misspell
    - unconvert
    - unparam

formatters:
  enable:
    - gofmt
    - goimports
